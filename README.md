# Aprovado AI - Monorepo

## Estrutura
- `/backend`: PHP/Laravel (API e Admin Blade Legado)
- `/frontend`: React/Vite (SPA)

## Docker
Utilize `docker-compose.yml` ou `docker-compose.local.yml` na raiz para orquestrar os serviços.
